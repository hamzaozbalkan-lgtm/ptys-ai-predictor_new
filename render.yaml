services:
  - type: web
    name: ptys-ai-predictor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn predictor_app:app --host 0.0.0.0 --port $PORT
    plan: free
